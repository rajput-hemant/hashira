import { Inter, Overpass } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const overpass = Overpass({
  subsets: ["latin"],
  display: "swap",
});
